# Session 2026-06-27

## Work Done
- Fixed wikidata_query entity mode User-Agent bug (HTTP 403)
- Fixed MCP handler JSON double-escaping (json.RawMessage passthrough)
- Fixed MCP handler Content-Length header (chunked encoding issues)
- Fixed MCP handler protocol version negotiation
- Added outputSchema to Tool struct (then removed — caused issues with llama-server MCP client)
- Cleaned up test files
- Updated memory.md with all fixes

## Lessons Learned
- outputSchema on MCP tool definitions confuses some clients (llama-server reported 0 tools)
- Content-Length header is critical for MCP clients that don't handle chunked encoding
- Protocol version negotiation prevents client compatibility issues
- Always use curl.exe for testing on Windows (PowerShell alias mangles JSON)

## Current State
- All 22 tools working (20 verified, 2 marked as JSON-returning which is correct)
- MCP server fully functional
- llama-server connects and uses tools correctly
